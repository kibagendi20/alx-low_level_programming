#include "main.h"
/**
 * add - adds two integers
 * @a: number to be summed with b
 * @b: added to a
 * Description: Adds the two operands and stores the answer to sum var
 * Return: summation of the operands
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
