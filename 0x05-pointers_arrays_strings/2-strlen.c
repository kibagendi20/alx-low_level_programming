#include "main.h"
/**
 * _strlen - Returns the length of a string.
 * @s: The string variable
 * Return: The length of @str.
 */
int _strlen(char *s)
{
	int count = 0;

	while (*s != '\0')
	{
		count++;
		p++;
	}

	return (count);
}
